{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @poc-3/* --parallel start",
    "build": "lerna run --scope @poc-3/* build",
    "serve": "lerna run --scope @poc-3/* --parallel serve",
    "clean": "lerna run --scope @poc-3/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  },
  "workspaces": {
    "packages": [
      "shell",
      "aside",
      "main",
      "header"
    ]
  }
}
