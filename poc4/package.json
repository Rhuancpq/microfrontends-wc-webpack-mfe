{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @poc-4/* --parallel start",
    "build": "lerna run --scope @poc-4/* build",
    "serve": "lerna run --scope @poc-4/* --parallel serve",
    "clean": "lerna run --scope @poc-4/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  },
  "workspaces": {
    "packages": [
      "shell",
      "aside",
      "main",
      "header",
      "footer"
    ]
  }
}
