{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @poc-1/* --parallel start",
    "build": "lerna run --scope @poc-1/* build",
    "serve": "lerna run --scope @poc-1/* --parallel serve",
    "clean": "lerna run --scope @poc-1/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  },
  "workspaces": {
    "packages": [
      "shell",
      "microfrontend"
    ]
  }
}
